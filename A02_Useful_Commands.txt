# 1. General operation
# Allow importing modules across different /Zxx folders
$env:PYTHONPATH = "."

# 2. Virtual environment
# Allow administrator privileges
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

# Activate virtual environment
.\pivdataprocessor\Scripts\Activate.ps1

# 3. Git operation
# Discard all local modifications and reset to the current released version
git fetch origin
git reset --hard origin/main